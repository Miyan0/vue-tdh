// module application
// manage the entire application state

enum SidebarType {
  FULL = 0,
  MINI,
  NONE
}

const state = {
  sidebarType: SidebarType.FULL
}

const getters = {}

const actions = {}

const mutations = {}

export default {
  state,
  getters,
  actions,
  mutations,
}

